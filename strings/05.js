const producto = "monitor 2 pulgadas";
// replace reemplaza
console.log(producto.replace("monitor", "curvo"));
// slice corta
console.log(producto.slice(0, 2));
// substring
console.log(producto.substring(0, 4));
