const producto = "monitored";
const precio = "30 usd";

/* console.log(pr1.concat(precio));
console.log(pr1.concat(" hla")); */

console.log(producto + " precio de: " + precio);
console.log("producto", producto, " de ", precio);

console.log(`el producto ${producto} tiene ${precio}`);
