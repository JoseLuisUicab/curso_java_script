const producto = "monitor 20 pulgadas".repeat(5);
// repeat perite repetir una cadena de texto
console.log(`${producto}`);

// split dividir un string
const activity = "natalia garcia fernandez";
console.log(activity.split(" "));

const leer = "muscia,hola.amor,prog";
console.log(leer.split(","));
